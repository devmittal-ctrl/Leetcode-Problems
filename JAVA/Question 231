class Solution {
    public boolean isPowerOfTwo(int n) {
        // power of 2 
        if(n<0) return false;
        if(n>=0){
        for(int i=0;i<=31;i++){
           long check=(long)Math.pow(2,i);
            if(check==n) return true;
        }}
        return false;
    }
}
